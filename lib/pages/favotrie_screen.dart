import 'package:flutter/material.dart';

class FavotrieScreen extends StatelessWidget {
  const FavotrieScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(child: Text("FAVORUTE SCRREN")),
    );
  }
}
